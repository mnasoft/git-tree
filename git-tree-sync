#!/bin/bash

# 👉 Функция справки
usage() {
    echo -e "\n🧰 USAGE: $0 [-all | <кол-во_дней>] [-bare <директория>] [-compress] [--dry-run]"
    
    echo -e "\n🔍 Описание:"
    echo "  Сканирует все подкаталоги на наличие Git-репозиториев и выводит дату последнего коммита."
    echo "  Можно фильтровать по возрасту коммита (в днях), создавать bare-репозитории,"
    echo "  архивировать их в .tar.xz и выполнять dry-run для симуляции."
    echo "  Все действия логируются в файл scan.log."

    echo -e "\n📌 Параметры:"
    echo "  -all            — показать все репозитории (эквивалент DAYS=99999)"
    echo "  <кол-во_дней>   — число дней для фильтрации по дате последнего коммита"
    echo "  -bare <dir>     — путь к каталогу, куда будут скопированы bare-репозитории"
    echo "  -compress       — включает сжатие каждого bare-репозитория в архив"
    echo "  --dry-run       — симуляция действий без их выполнения"
    echo "  --help          — показать это сообщение"

    echo -e "\n📋 Примеры:"
    echo "  $0 -all"
    echo "  $0 30"
    echo "  $0 30 -bare /mnt/usb/git-sync"
    echo "  $0 30 -bare /mnt/usb/git-sync -compress"
    echo "  $0 30 -bare /mnt/usb/git-sync -compress --dry-run"
}

# 👉 Проверка на пустые параметры или --help
if [[ $# -eq 0 || "$1" == "--help" ]]; then
    usage
    exit 0
fi

# 🧩 Разбор аргументов
DAYS=""
BARE_DIR=""
COMPRESS=false
DRY_RUN=false
LOGFILE="scan.log"

while [[ $# -gt 0 ]]; do
    case "$1" in
        -all)
            DAYS=99999
            shift
            ;;
        -bare)
            BARE_DIR="$2"
            shift 2
            ;;
        -compress)
            COMPRESS=true
            shift
            ;;
        --dry-run)
            DRY_RUN=true
            shift
            ;;
        --help)
            usage
            exit 0
            ;;
        *)
            DAYS="$1"
            shift
            ;;
    esac
done

# Проверка DAYS
if [[ -z "$DAYS" ]]; then
    echo "❌ Не указано количество дней. Используй -all или число."
    usage
    exit 1
fi

# Проверка bare-директории
if [[ -n "$BARE_DIR" && $DRY_RUN == false ]]; then
    mkdir -p "$BARE_DIR" || { echo "❌ Не удалось создать каталог $BARE_DIR"; exit 1; }
fi

tempfile=$(mktemp)
now=$(date +%s)

echo -e "\n===== Сканирование $(date '+%Y-%m-%d %H:%M:%S') (порог: $DAYS дней) =====" | tee -a "$LOGFILE"

find . -type d -name ".git" | while read gitdir; do
    repo_dir=$(dirname "$gitdir")
    cd "$repo_dir" || continue

    last_commit_date=$(git log -1 --format="%ci" 2>/dev/null)

    if [ -n "$last_commit_date" ]; then
        commit_ts=$(date -d "$last_commit_date" +%s)
        age_days=$(( (now - commit_ts) / 86400 ))

        if [ "$age_days" -le "$DAYS" ]; then
            echo "$last_commit_date|$repo_dir" >> "$tempfile"

            if [[ -n "$BARE_DIR" ]]; then
                repo_name=$(basename "$repo_dir")
                target="$BARE_DIR/$repo_name.git"
                archive="$BARE_DIR/$repo_name.git.tar.xz"

                if $DRY_RUN; then
                    echo "🧪 [dry-run] Создал бы bare-репозиторий: $target" | tee -a "$LOGFILE"
                    if $COMPRESS; then
                        echo "🧪 [dry-run] Архивировал бы: $archive" | tee -a "$LOGFILE"
                    fi
                else
                    echo "📦 Создаю bare-репозиторий: $target" | tee -a "$LOGFILE"
                    git clone --bare "$repo_dir" "$target" 2>/dev/null

                    if $COMPRESS; then
                        echo "🗜️ Архивирую: $archive" | tee -a "$LOGFILE"
                        tar -cJf "$archive" -C "$BARE_DIR" "$repo_name.git"
                        rm -rf "$target"
                    fi
                fi
            fi
        fi
    fi

    cd - > /dev/null
done

# 📋 Вывод
echo -e "\n📋 Репозитории с последним коммитом не старше $DAYS дней:\n" | tee -a "$LOGFILE"
sort "$tempfile" | while IFS="|" read date path; do
    echo "🕒 $date → 📁 $path" | tee -a "$LOGFILE"
done

rm "$tempfile"
echo "✅ Завершено. Лог сохранён в $LOGFILE"
