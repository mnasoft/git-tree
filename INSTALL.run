#!/bin/bash

echo "" > INSTALL.log
echo "" > UNINSTALL.run

mkdir -p /usr/local/bin/
for i in `find . -name "git-*"`
do
    cp -f ${i} /usr/local/bin/
    echo "/usr/local/bin/${i}" >> INSTALL.log
    echo "rm -f /usr/local/bin/${i}" >> UNINSTALL.run
    chmod +x UNINSTALL.run
done

for i in `find . -name "clp-*"`
do
    cp -f ${i} /usr/local/bin/
    echo "/usr/local/bin/${i}" >> INSTALL.log
    echo "rm -f /usr/local/bin/${i}" >> UNINSTALL.run
    chmod +x UNINSTALL.run
done
